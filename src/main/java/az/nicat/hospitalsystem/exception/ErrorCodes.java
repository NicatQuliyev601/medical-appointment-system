package az.nicat.hospitalsystem.exception;

public enum ErrorCodes {
    EMAIL_ALREADY_EXIST,
    USERNAME_ALREADY_EXIST,
    AUTHORITY_NOT_FOUND,
    USER_NOT_FOUND,
    INVALID_PASSWORD,
    EMAIL_NOT_CONFIRMED
}
